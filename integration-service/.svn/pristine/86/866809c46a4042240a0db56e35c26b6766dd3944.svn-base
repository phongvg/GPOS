package com.gg.gpos.integration.mapper;

import org.mapstruct.Mapper;

import com.gg.gpos.integration.dto.SyncArchiveDto;
import com.gg.gpos.integration.entity.SyncArchive;

@Mapper(componentModel = "spring")
public interface SyncArchiveMapper {
	SyncArchive dtoToEntity(SyncArchiveDto syncArchiveDto);
	SyncArchiveDto entityToDto(SyncArchive syncArchive);
}
