package com.gg.gpos.menu.repository;

import java.util.List;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;
import com.gg.gpos.menu.entity.So;

/**
 * Generated by Speed Generator
 * 
 * @author <a href="mailto:ngtrungkien@gmail.com">Kien Nguyen</a>
 */
@Repository
public interface SoRepository extends JpaRepository<So, Long>, JpaSpecificationExecutor<So> {
	List<So> findByStatusIsTrue();
	@Query("select s from So s where s.status=true and s.id in (select distinct sc.so.id from SoCategory sc where sc.restaurantCode is null)")
	List<So> findStatusAndSoCategories();
}